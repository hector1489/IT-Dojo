import { ShoppingCart } from "../../components/index";

const Cart: React.FC = () => {

    return (
      <div className='box-cart'>
        <ShoppingCart />
      </div>
    );
  };

export default Cart;